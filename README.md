# SQL Practice and exercises
##### SQL Cheatsheet
- `SELECT` 'столбцы или * дл€ выбора всех столбцов; об€зательно'
- `FROM` 'таблица; об€зательно'
- `WHERE` 'условие/фильтраци€, например, city = 'Moscow'; необ€зательно'
- `GROUP BY` 'столбец, по которому хотим сгруппировать данные; необ€зательно'
- `HAVING` 'условие/фильтраци€ на уровне сгруппированных данных; необ€зательно'
- `ORDER BY` 'столбец, по которому хотим отсортировать вывод; необ€зательно'
```sh
SELECT [DISTINCT | ALL]{*
 | [<выражение дл€ столбца> [[AS] <псевдоним>]] [,Е]}
FROM <им€ таблицы> [[AS] <псевдоним>] [,Е]
[WHERE <предикат>]
[[GROUP BY <список столбцов>]
[HAVING <условие на агрегатные значени€>] ]
[ORDER BY <список столбцов>]
```